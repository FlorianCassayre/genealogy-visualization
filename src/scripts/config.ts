import { join } from 'path';

export const INPUT_FILE = 'genealogy.ged';
export const PUBLIC_DIRECTORY = 'public';
export const OUTPUT_DIRECTORY = join(PUBLIC_DIRECTORY, 'data');

export const TREE_DEPTH_LIMIT = 9;
export const TREE_DEPTH_SENSITIVE = 3;
